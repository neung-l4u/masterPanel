# Website monitor — checks every 5 minutes and alerts the Google Chat space.
# Installed into the container by docker-compose (service: web).
*/5 * * * * root cd /var/www/html/assets/php && /usr/local/bin/php check_monitor.php >> /var/log/monitor_cron.log 2>&1
